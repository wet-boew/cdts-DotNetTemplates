@inject IJSRuntime jsRuntime

@code {
    [Parameter]
    public GoC.WebTemplate.Blazor.Model.PreFooter PreFooterObj { get; set; }

    protected override void OnAfterRender(bool firstRender) // = On Page Load
    {   
        jsRuntime.InvokeVoidAsync("cdtsBlazor.setPreFooter", JsonPlainSerializationHelper.SerializeToJson(PreFooterObj).ToString());
        jsRuntime.InvokeVoidAsync("cdtsBlazor.resetWetComponents", "wb-share");
    }
}
